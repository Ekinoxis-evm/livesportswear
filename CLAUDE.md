# Live — Project Memory for Claude Code

> This file is loaded into every Claude Code session. Keep it tight; long-form context lives in `.claude/rules/*`.

## Mission
Internal ops app for **Live Active Wear** (liveactivewear.com), in production at livesportswear.vercel.app. Three surfaces:
- **Admin**: weekly schedules (Mon–Sun) with a rules engine (hard limits block publish, soft ones warn), a Performance hub (Daily floor · Sales · Rewards), and Sales & Rewards setup (goals, commission tiers, contests, per-rep monthly goals).
- **Store kiosk** (shared iPad login, `role=store`): check-in/out with PIN + face photo, the rotation queue ("up system") with breaks/undo, sales contests leaderboard, close-day report.
- **Employee portal + public pages**: personal schedule/sales/rewards, magic-token schedule page + ICS feed, public store-week page.

**Sales metric everywhere = NET sales** (Shopify `current_subtotal_price`: after discounts/refunds, excluding taxes+shipping). Conversion counts live in `client_events`; money comes from Shopify.

## Stack (one-liner)
Next.js 16 (App Router) · TypeScript · Tailwind v4 + shadcn/ui (Base UI) · Supabase (Postgres + Auth) · Resend · Shopify Admin API · Vercel · pnpm.

## How this codebase is organized
- `src/lib/scheduling/` — pure scheduling functions: rules engine, conflicts, stats, week/payroll math. **No DB calls here.**
- `src/lib/` — other pure domain libs (same no-DB rule): `rewards.ts` (contest standings), `floor-queue.ts` + `floor-state.ts` (kiosk queue), `breaks.ts`, `commission.ts`, `conversion.ts`, `attendance.ts`, `shopify-range*.ts`, `monthly-series.ts`.
- `src/server/` — server actions and server-only assembly (the only place DB mutations happen). `*-core.ts` files hold shared bodies (floor, conversion).
- `src/lib/supabase/` — Supabase clients (server, browser, service-role).
- `src/app/admin/` — admin UI (role=admin). **Products** is a nav group with just **Catalog** (`/admin/products`) + **Inventory** (`/admin/inventory`) — the inventory sub-pages (Book, Push) are NOT sidebar entries; you move across them via the in-page **Count ▸ Book ▸ Push** funnel tab bar (`inventory-funnel.tsx`). Routes are unchanged; the `/admin/inventory` prefix keeps the group highlighted across `/book`, `/push`, `/[id]`. Sidebar groups the team routes under an **Employees** section (Profiles=`/employees` · Schedule=`/schedules` · Performance=`/performance` · Rewards & Commission=`/commission#…`) — a nav grouping only, routes are unchanged. Performance is a route-tab hub (`performance/{daily,sales,rewards}`); `/admin/commission` is the "Sales & Rewards setup" page; `/admin/clients` is the contact-attribution view; `/admin/rewards` is the live sales-contests page (linked from the sidebar, commission, and Performance→Rewards). The old `/admin/sales` redirect stub was removed in the 2026-07-25 audit.
- `src/app/portal/` — employee portal (role=employee): Performance / Schedule / Rewards / Settings. Performance is a route-tab hub in the `(performance)` route group — Overview (`/portal`, DB-only) · Sales (`/portal/sales`), period-driven. Clients (`/portal/clients`) is its **own** bottom-nav tab, not part of the hub.
- `src/app/store/` — the kiosk (role=store): Check-in / Schedule / Sales (center) / Performance / Rewards, 45s auto-refresh. Nav `<Link>`s are **`prefetch={false}`** on purpose — with prefetch on, each 45s refresh re-rendered all five routes server-side (~6–8 renders/cycle, measured in prod).
- `src/app/s/[token]/` + `src/app/w/[token]/` — public magic-token pages (employee schedule + ICS; store week + sales ranking).
- `src/app/api/cron/` — `shopify-sync` (also finalizes ended contests), `stale-checkins`, `photo-retention`, `meta-sync`. All check `CRON_SECRET`.
- `src/lib/emails/` — React Email templates (schedule-published, day-report, credentials, time-off-decision).
- `supabase/migrations/` — append-only, currently through `0060`. **Apply with `.claude/scripts/db-apply.sh <name>`** — never a hand-written curl (see `.claude/commands/db-migrate.md`).
- `.claude/` — agents, commands, hooks, rules, project skills, and `scripts/db-apply.sh` (the only sanctioned prod-DB write path).

## Coding standards (short version)
- Server-side mutations only via files in `src/server/`. Components never write to the DB.
- Zod validation at every boundary: server actions inputs, route handler payloads, env parsing.
- `validateSchedule()` from `lib/scheduling/rules.ts` runs at publish (`src/server/schedules.ts`) — never bypass.
- Magic tokens are 32-byte URL-safe random; **never log them**, never expose in error messages.
- Dates: store as `date` (no time) for shift dates; times as `time` in location-local; render with `date-fns-tz` and the location's IANA TZ. "Today" = `businessDate(tz)`.
- Complex create/edit forms use the shared `Wizard` shell (`src/components/shared/wizard.tsx`); side edits use Sheets; confirms use Dialogs.
- Don't add features beyond the current task. Don't add abstractions for hypotheticals. No backwards-compat shims.

## Never do
- `supabase db reset` without `--i-mean-it` (a pre-tool-use hook enforces this).
- Send real emails from dev. `RESEND_DRY_RUN=true` in `.env.local` for local work.
- Commit `.env.local`, `*.key`, or anything matching `*secret*`.
- Bypass RLS with the service-role client outside token-verified public routes, cron handlers, or admin/kiosk server actions (see `.claude/rules/security.md`).
- Write floor/queue state from anywhere but the kiosk server actions (`src/server/store-floor.ts`) — the kiosk is the floor's single writer.

## Where to find specifics
- Schema decisions: `.claude/rules/data-model.md`
- UI patterns (Dialog vs Sheet vs Wizard, color usage): `.claude/rules/ui-patterns.md`
- Security (roles, RLS, magic tokens, hardening): `.claude/rules/security.md`
- Testing (rules engine coverage gate): `.claude/rules/testing.md`
- Go-live keys checklist: `docs/ready-for-keys.md`
- Adding a scheduling rule: `/add-shift-rule` slash command
- **Applying a migration to prod**: NOT `supabase db push` (remote history uses
  timestamp IDs, files are numbered → the CLI can't reconcile). Use the Supabase
  Management API query endpoint — see `.claude/commands/db-migrate.md` for the
  exact `curl`. Needs operator approval (MCP is read-only; classifier blocks
  self-applied DB writes).

## Current state
In production since 2026-07. Live: scheduling + publish emails/ICS, time-off, store kiosk (PIN/photo check-in, FIFO turn order by available_since with bump/drag overrides, multi-client, breaks on the Check-in tab, undo, close-day report emailed as CSV + a 4-sheet **XLSX** workbook (Summary·Employees·Client events·Check-ins, per-employee net·orders·avg-ticket) + PDF), 5-tab kiosk nav (Check-in · Schedule · SALES center button · Performance · Rewards) with a read-only Schedule page (today/week), Shopify net-sales sync (10-min GitHub Action + daily cron) with 2024→now history, dashboards + year charts (month Ranking with commission lives on the Dashboard), the **sales-period module** (PeriodPills + SalesRankTable: Today · Week · Month · Custom — the canonical ranked-sales pattern on kiosk/dashboard/admin-sales/public week page), commission tiers, sales contests v3, per-rep monthly goals, per-location admins, security-hardened RLS helpers, **inventory suite** — two count types: **Counting** (barcode counts with confirm-scan + qty, product-type categories, camera scanning incl. iPhone via zxing ponyfill + guided scan zone, scanner-connection indicator, store inventory book, staged Shopify push draft→review→write with write_inventory) and **New Stock/receiving** (upload an arrival doc → CSV parse or AI vision extraction via the Vercel AI Gateway → match by barcode/SKU → physically verify → additive merge that adds arrived onto current Shopify on-hand), **sold→Shopify order + customer linking** (client-history seed: order/customer on client_events, shown in day reports and the admin Clients view with per-rep contact attribution), employee color palette picker + kiosk profile photos, a 5-area audit hardening pass (scoped store-account admin, atomic finish, true book replace, per-store portal rank, close-day idempotency), the **sepia rebrand** (app accent moved from forest green to the "Sepia Brown" palette — beige bg + deep-sepia CTAs + charcoal-brown sidebar, centralized in `globals.css`), **editable daily-report recipients** (`store_report_recipients`, managed on admin Performance→Daily *and* the kiosk close-day area via the shared `RecipientsManager`, with a "Send test report" button that surfaces real delivery failures), and the **kiosk today's-orders + attendance views** (`/store/performance`): a "Clients attended today" list from `client_events` (per attendee: sold/no-sale, contact, return, linked order + amount, customer) and an "Orders today" card of the day's real POS sales (`source_name='pos'` only — this Shopify store is POS-only; online lives on a separate shop; `shopify_draft_order`/other are excluded) with store total + each seller's orders · net · average ticket, attributed by `order.user_id`→`employees.shopify_staff_id` (`src/lib/orders-today.ts`, `fetchDayOrders`), a **return/exchange type** step on the kiosk finish flow (`client_events.return_type` — report label only, no metric change) with a set-apart Return/Exchange floor button, **kiosk responsiveness** work (route `loading.tsx` skeletons, parallel Shopify reads, `useLinkStatus` nav spinners, `touch-action: manipulation`), and the **portal Performance hub** — `/portal` split into Overview · Sales · Clients tabs so a rep sees what admin/kiosk already capture: all-time net sales / best month / monthly average + personal-goal bar + their own year chart (DB-only, no Shopify call), then per-period orders · average ticket · largest sale · best day · most orders in a day · rank (live Shopify per-order data via `getRangeOrdersCached` + the pure `src/lib/personal-stats.ts`; records are **within the selected period**, not all-time), and conversion + "clients in Shopify" where a client counts as **new** when their Shopify `created_at` falls inside the period they bought (Shopify records no created-by-staff, so attribution follows the sale) alongside the exact kiosk-captured contact list, and **client attribution** (`customer_origin`, 0042) — Shopify is the client book, so `/admin/clients` now lists Shopify's customers (searched via `customers/search.json`, hydrated per page) annotated with the rep who brought each one in, derived from that customer's first POS order back to 2024 rather than from kiosk linking; the kiosk Sold flow lost its quiet "Skip" in favour of an equal-weight "No customer on this sale" and a 12-order list.

**Since then (2026-07-22):** the **portal Clients tab** (`/portal/clients`, its own bottom-nav tab — it left the Performance hub, which is now Overview · Sales) giving each rep the book they built, with WhatsApp/email per client and a profile page (`/portal/clients/[customerId]`: spend, orders, average ticket, and every garment bought with product images). Contact links refuse a phone `countryFromPhone` can't place — `wa.me` needs the country code, and ~28% have no phone / ~46% no email, so "not reachable" is a normal, labelled state (`src/lib/contact-links.ts`). **Client country** (`customer_origin.country_iso`, 0044) derived from the phone's country indicator via **`libphonenumber-js`** + CLDR names + computed flags (`src/lib/phone-country.ts` — nothing hand-maintained, and it never guesses: unknown stays unknown). **Admin Products** (`/admin/products`) — the ~3,600-product Shopify catalog as a compact list, cursor-paginated, type filter in a `<details>`; inventory nests under it in the nav with **no route moves**. **Re-take client** on the kiosk (`src/lib/retake.ts`) — a client already attended today comes back and buys, and the sale is added to the attendance already logged (one attended, one sold; `order_total` **adds**, never replaces). **One report wizard** for both the test send and close-of-day (`src/components/store/report-wizard.tsx`): recipients → numbers → who's sending, recipients droppable **per send** and intersected server-side against the stored list (`src/lib/report-recipients.ts`). Closing marks `floor_days.closed_at` (0048) to end the day's queue and **deliberately checks nobody out** — the closer does this during their shift; everyone still taps their own PIN checkout. Money now formats **to the cent everywhere** (`formatMoney`), and every data table uses the `ScrollTable` shell (sticky header, pinned first column, scrolling-shadow edge).

**Since then (2026-07-25):** **multi-order sold + no-sale re-take** — a sold walk-in can link SEVERAL Shopify orders (`client_events.linked_orders` jsonb, 0049; the single `shopify_order_*` cols stay as the primary, `order_total` = sum; merge/dedupe rule in `src/lib/linked-orders.ts`), and re-take gained an outcome step so a client who came back but didn't buy is re-logged without a second walk-in (`retake.ts`, never downgrades a real sale). **Kiosk PII tightened** — the recent-orders pick-list no longer ships or stores customer email/phone (nothing read them; Shopify owns contact); `client_events.customer_email/_phone` go unwritten. **Goal indicator** (`src/components/shared/goal-indicator.tsx`) — the monthly-goal "left to reach + per-day pace" renders as a **fill-card** (the goal colour fills the card to the percent) on portal · kiosk · admin; pure math in `src/lib/goal-pace.ts`. **All-time shifts by weekday** — admin schedules gained a Totals ⇄ By-weekday toggle (`weekdayShiftGrid` in `lib/scheduling/shift-grid.ts`, `all-time-shifts.tsx`). **Schedule colour tint** — shifts render as a light tint of the employee's profile colour (not a dot) everywhere, via shared `src/lib/shift-color.ts` (`shiftTint`) + `ShiftChip`. **Sortable tables** — shared click-to-sort (`src/lib/use-table-sort.ts` `useTableSort`/pure `sortRows`; `SortableTh` + sortable `TableHead`); applied to the sales-rank leaderboard, kiosk attendance/orders/team-today, all-time-shifts Totals, admin marketing + performance-daily, the admin CRUD + inventory tables, and — server-side — the inventory book and the **client lists** (`ServerSortHead`/`ServerSortTh` + `?sort=&dir=`). The client lists sort by **cached Shopify stats** on `customer_origin` (`customer_name`/`orders_count`/`total_spent`, 0051; refreshed by `runCustomerStatsSync` on the cron + admin Rebuild), since those columns were fetched per-page and un-sortable; email/phone are still fetched live, never cached. **Every data table in the app is now sortable.**

**Client thank-you WhatsApp + workable-day pace (0052):** `message_templates` (per location + language pt/en/es, 0052) holds an admin-editable thank-you, edited in an expandable section of `/admin/clients` (`message-templates-card.tsx`). After a sold walk-in with a customer, the kiosk finish flow offers a thank-you step (3 language buttons) → `storeThankYouLink` resolves the client phone + line items server-side (`fetchOrderById`), fills the template (`{name}` + auto-appended products, pure `src/lib/thank-you.ts`), and returns a `wa.me` URL the kiosk opens. **A "use server" file may only export async functions** — the language const/type live in `src/lib/message-languages.ts`, not the action file (a clean build catches this; local incremental builds don't). The monthly **goal-pace "per day" is now over WORKABLE days for a person** (`goalPace(..., workDaysLeft?)` + `remainingWorkdays` in `lib/scheduling/workdays.ts`: published shifts today→month-end + a 5/7 tail estimate) on portal + admin per-rep; the store/kiosk pace stays flat calendar days.

**Store clients + message types + map + shift-count redesign (0053–0054):** a **`hello` message type** joined `thank_you` (message kinds 0053; `{last_product}` token; the builder is `src/lib/client-message.ts` `buildMessage`), both edited in the admin Client-messages card. A **store clients page** (`/store/clients`, a kiosk Clients tab) mirrors the admin book — list/search the location's `customer_origin`, a manual **`in_whatsapp`** checkbox (0054, kiosk-set via a location-scoped service action), and **send a hello/thank-you over WhatsApp** (`storeListClients`/`storeSetInWhatsapp`/`storeMessageLink`; phone + last purchase resolved server-side). The admin clients country card gained a **Flags ⇄ Map** toggle — a self-contained SVG world choropleth (`src/lib/world-geo.json`, slimmed Natural Earth 110m, no CDN; hand-rolled equirectangular projection in `clients-world-map.tsx`). The **weekly shift-count grid** was replaced with employees-columns × AM/PM/Total-rows (reuses `accumulatedShiftCounts` on the week's shifts); the all-time views are unchanged. **Audit fixes (0050)**: `setEmployeeAdmin` now requires master + scopes the new admin to their store; finishing a *return* no longer clears the bump; the admin dashboard scopes to `accessibleLocationIds`; dropped the legacy `attendance_validations` table; removed ~10 dead exports.

**Kiosk polish · admin clients tabs · inventory funnel + arrivals matrix (0055):** the kiosk center nav button reads **TURN** (was "Sales"); the Performance page's "Clients" sub-tab is now **Attendance**, table-first, its four square metric cards folded into a totals strip on the table (`attendance-today.tsx`). **`/admin/clients` split into 3 route-tabs** — Database (list + flags/map + search), Messages (`MessageManager`, kinds × languages; adding a kind is one `KINDS` array entry), Attribution (rebuild + per-rep stats) — mirroring the `admin/performance/` hub pattern. **Inventory is one guided funnel**: a shared `<InventoryFunnel>` strip (Count ▸ Book ▸ Push, current highlighted, steps linked) on the landing/session/book/push pages + a "Go to inventory book" CTA on finalized sessions, so the three screens read as one flow (no schema). **New-arrivals matrix** (0055): receiving extraction now also reads the **HS code** (vision prompt + CSV `HS_KEYS`; stored `inventory_count_items.hs_code`), and the verify step is a **reference × size matrix** (`matrixView`, `receive-matrix.tsx`) reconstructed from the SKU — HS · description · qty-per-size · row total + a stats bar (references · pieces) — where a rep **ticks each reference verified** (`verified` col; `setReferenceVerified` accepts the doc qty as arrived) before booking; blind Counting is unchanged.

**Floor return visibility · receiving reference-match + kiosk counting (0058):** on the kiosk floor, a rep on a **return** now reads clearly in "The line" (stronger amber ring + amber position number) so you can watch them climb to their turn — mechanics unchanged (they already keep their `available_since` spot); the **Return/Exchange + Re-take buttons** sit side-by-side, equal height, filled backgrounds. **Receiving reading step** shows the **reference × size matrix** (`extractMatrix`, pure) — per-size cells, row total, grand total of pieces. **Matching is by reference** (`buildReferenceIndex`/`matchByReference`): Shopify SKUs are `reference.SIZE.color`, so the old exact-SKU match missed whenever the doc's color text ≠ Shopify's color code — now it's exact barcode/SKU → **reference + size** (color-lenient; color only disambiguates a multi-color reference), misses carry a hint, unmatched route to "Other lines". **Receiving is now a 3-role flow (0058):** admin uploads + matches → **`sendToKioskCounting`** hands it to the store iPad → **employees count the arrival per size** at **`/store/receiving`** (new area, surfaced as a layout banner; `src/server/store-receiving.ts` service-client actions scoped to the JWT location + a `counting` session) → **`storeMarkReadyToPush`** → **admin reviews + pushes** (`receiveStock`, additive `onHand + counted`, admin-only). `inventory_counts.status` gained `counting`/`ready` (0058; the one-active-session index widened to `status <> 'final'`); `reopenReceiving` pulls a count back to `open`. The old admin-only `setReferenceVerified` + `receive-matrix.tsx` were removed. **Kiosk fixes:** a rep on a return can now **take a walk-in from their line row** (was only reachable on the up-next hero — the server always allowed it); a **permanent "Receiving" kiosk nav tab** (the counting banner stays as the act-now alert).

**One goal meter + one commission-setup panel (no schema):** the three stacked goal visuals (inline bar + `GoalIndicator` fill-card + `GoalLevelsBar`, plus the portal's `TierLadder`) collapsed into ONE **`GoalMeter`** (`src/components/shared/goal-meter.tsx` + pure `src/lib/goal-meter.ts` `buildGoalMeter`): a single bar `0 → top level`, **emerald fill on reach**, a tick per commission level (+ the set goal), and — **always on, no tap/hover** (so it's a plain server component, not `"use client"`) — a **"$X more · $Y/day → next level"** line then the tiers as tidy rows (reached ✓, next highlighted, target + rate). Used on kiosk Performance (store levels), portal (per-rep tiers), admin Perf→Sales (`compact`); the old three components were deleted. The **commission setup** (`/admin/commission`) collapsed its 3 cards into one **`MonthlySetup`** panel under a **single month picker** (was two that could drift): tiers → personal goals (**"Fill all from first tier"**) → store goal (**"Set to sum of personal goals"**) → a live coherence strip, one "Save this month" (calls `setStoreMonth` + `setEmployeeGoals`). Reflects the model: personal goal = first paid tier, store goal = Σ personal goals. `StoreMonthForm`/`EmployeeGoalsForm`/`GoalsOverview` removed; Global default tiers card kept. **Inventory nav + delete:** the Products sidebar group dropped Book + Push (now reached via the in-page funnel tab bar); **delete a process** — `deleteCount` widened from open-only to any non-final session (open/counting/ready) so an in-progress count OR receiving can be cancelled (finalized stays immutable, items cascade, best-effort clears the uploaded doc), wired to a trash button + confirm on the sessions table and the session page.

**Shopify-exact sales breakdown + Total sales (0059):** the sales money now mirrors Shopify Analytics label-for-label — **Gross sales − Discounts − Returns = Net sales; + Taxes = Total sales** — sourced straight from the order fields (verified to the penny in a 14-day live reconciliation). `SalesBreakdown` (`src/lib/sales-breakdown.ts`) gained `taxes` (`total_tax`) + `total` (`net + taxes`; shipping ~0 for this POS-pickup store). `monthly_sales.tax_amount` (0059) carries tax for the Month/all-time views (Total = `amount + tax_amount`; null pre-0059 months fall back to Net until re-synced). Those exact labels + the new Taxes/Total show across `SalesBreakdownBlock`, `SalesRankTable` and the day report (email/PDF/XLSX). **Net stays THE metric** (goals/commission/contests unchanged); Total is the register-reconciliation figure. `DaySales.total` now means Total sales (was a net alias — avg-ticket reads `.net`). Caveat: returns are dated to the order date, not the refund date as Shopify Analytics does.

**Kiosk/clients/performance batch + attend timer (0056–0057):** **field fixes** — kiosk Attendance leads with the per-employee Team-today table (totals strip on its header), the event log below; **returning from a break sends you to the BACK of the line** (`storeEndBreak` restamps `available_since`); the close-day report is **multi-signatory** (pick several on-floor reps; `closed_by` stays the first, the email signs `joinNames(...)`); kiosk `/store/clients` is a **columnar sortable table** (Client · Country · Value, server-side `?sort=&dir=`) with a **rep filter** + **"brought in by"** column. **Portal Clients can message** (hello/thank-you) like the kiosk via a shared `ClientMessageDialog` + `portalMessageLink` (own-client RLS-scoped, signed as the rep). **`{signature}` token** in `message_templates` fills the rep's name in place (the 6 template bodies use it; their footer now says **liveactivewear.com**, the brand's real site). **Commission tier gaps** — pure `src/lib/tier-gaps.ts` + shared `TierLadder` (portal: full per-tier ladder w/ per-workday pace; kiosk month view: compact per-rep "to next tier"). **On-demand sync buttons** — shared `SyncSalesButton` (`runShopifySync`→monthly_sales) + `RefreshClientsButton` (bounded attribution/stats sync) on kiosk/portal/admin, best-effort cooldown (`src/lib/action-cooldown.ts`). **Public week-share page** (`/w/[token]/[week]`) gained a **daily stacked-sales chart** (one bar per weekday, height = store net, stack = each rep's `avatar_color`, muted "Other" cap; `src/lib/week-sales-chart.ts` + `WeekSalesChart`, one Shopify read shared with the ranking). **Gross is the primary displayed per-employee sales figure** everywhere (ranking "Value", kiosk today's-orders, day report) — **NET stays THE metric** (goals/commission/contests unchanged); `DayOrder.gross`=`total_line_items_price`. **Per-client attend timer (0056)** — `client_events.served_seconds` + `floor_checkins.attending_started_at` jsonb queue (`src/lib/attend-timer.ts`, pushOpen/popOldest FIFO/popNewest/oldestAt); take stamps the start, finish pops oldest-of-kind → duration; **live `ClientTimer`** (black text, mirrors `BreakTimer`) on the `/store` board; **avg time / client** (`conversion.ts` `avgServedSeconds`/`formatDuration`) on kiosk Attendance strip + per-row Duration + Team-today, portal "This month", admin dashboard + Perf→Daily, and the daily report. **`client_events.attend_started_at` (0057)** persists the raw turn-start (audit; finish=`attended_at`, elapsed=`served_seconds`).

**Schedule drag-and-drop + mixer · kiosk next-tier columns + Orders tab (no schema):** the admin **schedule board is drag-and-drop** — each shift chip has a grip handle (dnd-kit, same pattern as the kiosk queue); drop on an empty cell **moves** the shift, drop on another chip **swaps** the two employees (`moveShift`/`swapShifts` in `src/server/shifts.ts`, both RLS-scoped), and name chips render on **one line** (wider day columns). The **schedule mixer** (`src/components/schedule/mixer-wizard.tsx`, reuses the `Wizard` shell) auto-builds a week two ways — from near-zero or to finish a started week — in 3 steps: **who's in** (all checked, confirm) · **limits** (per-employee maxDays/daysOff + per-slot headcount, pre-filled, **this run only**) · **generate & review** (Complete keeps existing / From-scratch clears, smart default; live preview grid with amber gaps; **Reshuffle**). The generator is pure + seeded (`src/lib/scheduling/generate.ts` `fillSchedule`, mulberry32 → deterministic so the preview matches the applied week; one shift/person/day, cap `min(maxDays, 7−daysOff)`, skips approved time-off, least-loaded-first spread, under-staffed → gaps not violations; **100% coverage**), and `applyMixer` (`src/server/schedules.ts`) re-runs the SAME generator server-side. On the **kiosk Performance → Sales (Month)** view the "to next commission tier" info moved from the block below the table INTO the table as two columns — **To next tier** (net still needed to unlock the next rate + the target rate beneath, "top tier" when maxed) and **Per day** (that gap ÷ calendar days left) — sourced from `commissionFor(net, tiers).nextTier`, wired via `SalesRankTable`'s `showNextTier` group + optional `SalesRankRow.nextTier`; the tier view **drops the Gross/Discounts/Returns/Taxes breakdown columns** (`showBreakdown` gate) so the tier columns fit the narrow kiosk without scrolling (breakdown stays in the summary block + day report). The day's orders now live in their own **Orders** tab (Performance = Sales · Orders · Attendance · Close day). The old `NextTierList` was removed. **Master admins from the UI (0060)** — `inviteAdmin` takes a `master` flag and stored temp passwords live in `admin_credentials` (default-deny, service-role), so a master can create another master and re-copy/reset an admin's password from Settings.

**Liveness probe + a real pre-commit secret hook (no schema):** on 2026-08-03 the Supabase project paused on idle and the store was down ~12¾h before anyone noticed — `/login` kept returning 200 from the CDN throughout, so nothing that only checks "did a server answer" would have caught it. **`/api/health`** (`src/app/api/health/route.ts`) actually round-trips Postgres (anon `select` on `locations`; RLS returns `[]`, which still proves the round trip) and returns **200 / 503** with `no-store`, so an uptime checker sees it. It's **excluded from the proxy matcher** (`src/proxy.ts`) because that proxy calls `auth.getUser()` on every matched request and therefore throws when Supabase is down — the probe has to outlive the thing it reports on. Verified both ways: green 200 against the real DB, red 503 against an unresolvable host while `/login` still returned 200. A failure takes ~7s (DNS), so monitors need a **≥15s timeout**. **`.githooks/pre-commit`** blocks staged env files, private-key material and secret-shaped content (Supabase JWT/`sbp_`, Resend `re_`, Shopify `shpat_`/`shpss_`, PEM); it lives in the repo (not `.git/hooks/`) and `pnpm install` wires it via `prepare` → `core.hooksPath`. Patterns are anchored on a non-word boundary so `require_foo` isn't read as a Resend key. `security.md` had claimed such a hook existed since v1; it never did.

**Coworkers on the published-schedule email + calendar event (no schema):** each shift in the publish/resend email now has a **"With"** column naming who else is on that shift, and the `.ics` event carries the same as its `description` ("With: …"). "Sharing" = **overlapping hours** — the other employees whose same-day shift overlaps in time (pure `overlappingCoworkerNames` in `src/lib/coworkers.ts`, reusing `overlaps()` from `scheduling/conflicts` — no scheduling-gate change). `EmailCtx` carries the week's shifts + a name map; `resendScheduleEmail` + the public ICS route (`s/[token]/calendar.ics`) load the whole location's published shifts so the subscribed feed matches the emailed one.
